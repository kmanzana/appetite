<!-- <span class="item well">
		<div><strong><%= link_to dish.name, restaurant_dish_url(dish.restaurant_id, dish.id) %></strong></div>
		<div><em><%= dish.price %></em></div>
		<% if session[:user_id] %>
		<% end %>
</span>
 -->
<div class="item">
	<%= link_to dish.name, restaurant_dish_url(dish.restaurant_id, dish.id), class: 'dish-link' %>
	<%= link_to image_tag(dish.image_url), restaurant_dish_url(dish.restaurant_id, dish.id) %>
</div>
